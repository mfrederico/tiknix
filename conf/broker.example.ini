; Auto-managed by tiknix — do not edit or commit. This is the RESET TEMPLATE that
; provisioning copies over conf/broker.ini so a freshly-cloned instance can never
; inherit the source instance's broker key.
;
; The broker key is a per-instance capability, bound to THIS instance's id, that lets
; the instance's app reach ITS OWN connected stores through the control-plane MCP
; gateway. It is populated automatically the first time you connect a store/account
; from the Connections page (BrokerService::ensureInstanceConfig mints a key scoped to
; this instance's id and rewrites this file). Until then, an empty key = no broker
; access — which is the correct, safe default.

[broker]
endpoint = ""
key = ""
